[package]
name = "prism"
version = "1.0.3"
description = "Prism — tournament integrity tool for Roblox FFlag detection"
authors = ["TSBCC"]
edition = "2021"
default-run = "prism"
autobins = false

[workspace]
members = ["."]
default-members = ["."]
resolver = "2"

[[bin]]
name = "prism"
path = "src/main.rs"

[lib]
name = "prism_lib"
crate-type = ["cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [], optional = true }

[features]
default = ["ui"]
# Pulls in the Tauri runtime + plugins. Disable when running cargo tests
# on environments without GTK / WebView2 system libraries.
ui = [
    "dep:tauri",
    "dep:tauri-plugin-opener",
    "dep:tauri-plugin-dialog",
    "dep:tauri-build",
    "dep:reqwest",
]

[dependencies]
tauri = { version = "2", features = [], optional = true }
tauri-plugin-opener = { version = "2", optional = true }
tauri-plugin-dialog = { version = "2", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.38"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
chrono = { version = "0.4", features = ["serde"] }
walkdir = "2"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
# Fast multi-pattern search + byte skip + work-stealing parallelism.
# Used by the file scanner to turn an O(N × patterns) tool-marker search
# into O(N), and to fan per-file content scans across CPU cores.
aho-corasick = "1"
memchr = "2"
rayon = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"], optional = true }
